# ─── docker-compose.override.yml.example ────────────────────────
# Copie para docker-compose.override.yml e customize.
# Este arquivo NÃO deve ser versionado (está no .gitignore).
#
# Valores aqui sobrescrevem ou estendem o docker-compose.yml.
# https://docs.docker.com/compose/extends/

services:

  wordpress:
    environment:
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_DEBUG', true);
        define('WP_DEBUG_LOG', true);
        define('WP_DEBUG_DISPLAY', true);
        define('WP_REDIS_HOST', 'redis');
        define('SAVEQUERIES', true);
    # Expor porta 9000 para debug com xdebug
    # ports:
    #   - "9000:9000"

  db:
    ports:
      - "3306:3306"

  phpmyadmin:
    # Forçar ativação do phpMyAdmin
    profiles: []
